# PushLedger 인수인계

푸시 알림을 읽어 자동 기록하는 안드로이드 가계부. Kotlin + Jetpack Compose.

- **프로젝트 루트**: `C:\Users\denni\OneDrive\문서\pushledger`
- **원격**: https://github.com/aidennis0297-art/ledger (main)
- **APK 배포 주소**: https://github.com/aidennis0297-art/ledger/raw/main/pushledger.apk
- **설계·화면 구성·알고리즘의 "왜"**: `README.md` — **먼저 읽을 것**

README 에 있는 내용은 여기서 반복하지 않는다. 이 문서는 **지금 상태의 좌표, 검증된
범위, 깨면 안 되는 불변식, 밟으면 시간 나가는 함정, 사용자와 일하는 법**만 적는다.

---

## 1. 현재 상태 (2026-09-08 실측)

소스 32개 파일 + 테스트 8개 파일. **유닛 테스트 103개 전부 통과.**
`assembleDebug` · `assembleRelease` 성공, **컴파일 경고 0개.** APK 13.5MB.
경고가 하나라도 뜨면 그건 새로 생긴 것이니 그 자리를 볼 것.

**테스트 수는 반드시 세어 보고 적을 것.** 이 세션에서 세 번 틀리게 적었다. 두 번은
스크래치패드에 둔 임시 프로브가 섞여 들어와서고, 한 번은 그냥 더하기를 틀렸다.
믿을 수 있는 값은 저장소 쪽에서 센 이것뿐이다:

```bash
grep -rho '@Test' app/src/test | wc -l
```

| 영역 | 파일 |
|---|---|
| 도메인 | `Store.kt` 991 · `Coach.kt` 531 · `Nvidia.kt` 530 · `Stats.kt` 477 · `Parser.kt` 393 · `MainActivity.kt` 331 · `Model.kt` 272 · `DailyWidgetProvider.kt` 245 · `NotifListener.kt` 221 · `AiWorker.kt` 205 · `StatusNotifier.kt` 189 · `Merchant.kt` 125 · `AiReview.kt` 121 · `Hangul.kt` 77 · `AiJob.kt` 58 · `Money.kt` 43 |
| 화면 | `ui/Inbox.kt` 649 · `ui/Ledger.kt` 635 · `ui/Charts.kt` 607 · `ui/Budget.kt` 512 · `ui/Home.kt` 434 · `ui/AiScreen.kt` 397 · `ui/Settings.kt` 393 · `ui/StatsScreen.kt` 386 · `ui/StatsYear.kt` 186 |
| 디자인 기반 | `ui/Dots.kt` 420 · `ui/Field.kt` 161 · `ui/EmptyState.kt` 104 · `ui/Burst.kt` 82 · `ui/Text.kt` 73 · `ui/Type.kt` 49 · `ui/Money.kt` 53 |

### 실기기에서 확인된 것 (사용자가 폰에서 직접 봤다)

- **NVIDIA API 실제 호출.** 모델 `deepseek-ai/deepseek-v4-flash-0731`. 동작한다.
  AI 기능 전체(알림 분석·기록 검토·리포트)가 걸려 있던 단 하나가 풀렸다.
- **상태창 알림.** 실제로 뜬다.
- **한글 픽셀 폰트.** 실기기에서 어떻게 보이는지 확인했다.
- **홈 위젯.** 홈 화면에 뜬다. 한동안 `"위젯을 추가할 수 없습니다"` 로 막혀 있었고,
  원인은 §4-11 에 적었다.

### 실기기에서 아직 확인되지 않은 것 — 가장 중요한 미검증 영역

사용자는 APK 를 폰에 설치해 쓰고 있지만, 아래는 **한 번도 확인하지 못했다.**

- **위젯을 크기별로 늘렸다 줄였을 때의 모습.** 1x1 부터 큰 칸까지 한 레이아웃으로
  받고, `WidgetPlanner` 가 칸 크기를 보고 아래 줄부터 접는다(§5). 경계값은
  `WidgetPlannerTest` 8개로 JVM 에서 검증했지만, **런처가 실제로 어떤 dp 를 주는지는
  기기마다 다르다.** 도트가 통째로 사라지거나 아랫줄이 잘리면 그 경계를 의심할 것.
- **위젯 큰 숫자 잘림.** 좁은 칸에서는 `4만 5000원` 대신 `4만` 꼴로 줄이고 글자 크기도
  칸에 따라 14~20sp 로 바꾼다. 실제로 안 잘리는지는 미확인.
- **스크롤 성능.** 화면의 모든 글자가 폰트 렌더링이라 문제없을 것으로 보지만 미측정.

### 실기기 데이터가 밝혀 준 것 (2026-09-06 알림 기록 6,955건)

사용자가 `알림 기록 CSV` 를 보내 줬다. 원문을 보고서야 알게 된 것 둘이다.

- **`명한돌` 은 버그가 아니었다.** 이 문서에 "식당 이름으로 보이는데 수입이 됐다" 고
  적혀 있었지만, 원문은 토스 알림 `43,333원 입금 / 명한돌 → 내 토스뱅크 통장` 이다.
  **명한돌은 사람이다**(인스타 친구). 실제로 돈을 보냈고 파서는 맞게 읽었다.
  더치페이 정산으로 보인다(43,333 = 130,000 ÷ 3). **원문 없이 화면만 보고 "식당 같다"
  고 적은 추측이 다음 사람을 몇 세션 동안 헛다리 짚게 만들었다.** 못 본 것은
  "못 봤다" 고만 적을 것.

- **삼성 월렛의 `₩7,100 결제 완료` 를 못 읽고 있었다.** `Parser.MONEY` 가 `원` 글자만
  봐서 원 기호로 온 금액이 전부 '금액 못 찾음' 으로 떨어졌다. 지금은 읽는다.
  **다만 이것 때문에 돈이 빠져 있던 것은 아니다** — 같은 결제가 토스에서도 오고
  그쪽은 이미 기록됐다. 18건 전부 가계부에 있었다. 처음에 "242,800원이 통째로
  빠져 있다" 고 적었는데 **가계부와 대조하지 않고 알림 기록만 보고 낸 결론이라 틀렸다.**
  숫자를 말하기 전에 반대쪽 파일과 맞춰 볼 것.

- **그 수정이 오히려 중복을 만들 뻔했다.** 삼성페이와 토스는 같은 결제를 각자 알린다.
  18건 중 15건은 2초 안에 붙어 오지만 셋은 **58초·59초·85초** 벌어져, 10초짜리 중복
  판정 창을 넘는다. `₩` 를 읽게만 하고 끝냈으면 그 셋이 두 줄로 들어왔다.
  그래서 `Store.DUP_WIDE_SEC`(180초) 를 뒀다 — **넓은 창에서는 가맹점까지 같은지를
  함께 본다.** 10초 창은 앱마다 가맹점 표기가 달라서 일부러 이름을 안 보는 창이라
  그대로 두고, 넓히는 쪽에만 이름 조건을 붙였다.
  실기기 로그를 시간순으로 다시 흘려 확인했다: 막힌 중복 18건, 새로 생긴 중복 0건.

- **월세 580,000원이 두 줄로 있었다.** 자동 자리표(`by="fixed"`) 한 줄과 손으로 넣은
  한 줄(`by="manual"`). 자리표는 소비 집계에서 빠지지만 손으로 넣은 줄은 안 빠지므로,
  같은 월세가 고정지출 계획으로 한 번 소비로 한 번 빠져 **하루 한도가 58만원만큼
  잘못 줄어 있었다.** 불변식 3 이 '등록 시점' 만 막고 있어서 등록 뒤에 손으로 넣는
  길이 열려 있었다. 이제 `addTxn` 이 **자리표가 남아 있을 때만** 그 자리표를 새 건으로
  갈아 끼운다. 이미 실제로 나간 뒤 같은 이름으로 또 결제하면 그건 그냥 소비다.

### 그 기록을 보고 고친 오분류 (테스트에 원문 그대로 들어 있다)

- **편의점 7건이 `기타/기타지출` 이던 것** — `씨유` 처럼 한글로 적힌 편의점이 분류
  규칙에 없었다. 넣을 때 `세븐` 만 따로 넣으면 안 된다 — 식비를 고정지출보다 먼저
  보므로 알뜰폰 `세븐모바일` 이 식비로 끌려간다.
- **`확인해주세요` 38,000원이 수입이던 것** — 카카오톡 정산 **요청**(내가 낼 돈)이
  `정산금액` 의 `정산금` 에 걸려 수입이 됐다. `Parser.SETTLE_REQUEST` 로 막는다.
  `정산 요청` 이라는 낱말 자체는 쓰면 안 된다 — 돈이 실제로 들어온 알림에도 붙는다.
- **`전자금융거래약관` 2,200원이 지출이던 것** — G마켓 캐시 소멸 안내다.
  본문에 "8월 28일까지 **사용** 가능" 이 있어 지출 규칙에 먼저 걸렸고, 광고 규칙은
  지출 규칙이 없을 때만 보므로 못 막았다. `Parser.VANISH` 를 지출 판정 앞에 뒀다.
- **가맹점 자리에 문장·코드·날짜가 앉던 것** — `완료되었습니다`, `..`(본문이 잘려
  마침표만 남음), `202608261416WEB010`, `2026년` 이 전부 가게 이름 행세를 했다.

### 두 번째 대조에서 나온 것 — 같은 기록을 규칙에 통째로 흘려 봤다

세는 방법을 먼저 만들었다(아래 "분류 품질을 재는 법"). 6,955건을 지금 규칙에 그대로
넣으니 지출 62건이 잡혔고, 그중 **16건(25%)이 기타** 였다. 하나씩 원문을 봤더니
분류가 문제가 아니라 **판정 자체가 틀린 것들이 섞여 있었다.**

- **취소로 잡힌 12건 중 7건이 취소가 아니었다.** `Parser.CANCEL` 이 `취소|환불` 이라는
  낱말 하나였기 때문이다. 걸린 것들:
  `※ 식품 특성상 취소 및 반품이 어려우니`(주문 확인서 맨 밑 안내),
  `송금 받기 전까지 ... 취소할 수 있어요`(송금했다는 알림),
  `(수신거부:...알림받기취소)`(광고 꼬리).
  **두 번째 꼴이 제일 나빴다 — 송금 세 건 73,100원이 지출로도 안 들어가고 사라졌다.**
  `SENT_BY_ME` 가 취소일 때는 비켜서기 때문이다.
  진짜 취소는 취소라는 말을 혼자 두지 않는다(`결제 취소`, `결제취소안내`, `승인취소`).
  무엇이 취소됐는지 붙어 있을 때만 취소로 본다. 12건 → 6건, 전부 진짜.

- **광고 28건.** 정보통신망법이 앞머리에 `(광고)` 를 붙이게 해 둔 덕에 한 낱말로
  가려낼 수 있다 — 결제 확인 알림에는 절대 안 붙는다. `NOT_SPEND` 에 "광고" 가 이미
  있었지만 그쪽은 결제 낱말이 없을 때만 본다. 광고 문구에 `사용 중인 번호 그대로` 나
  `특가` 가 섞여 결제 규칙이 먼저 걸렸다. 알뜰폰 광고의 `월 10원부터` 가 10원짜리
  지출로, 쇼핑몰 광고의 `17,800원 반값 특가` 가 17,800원 결제 취소로 들어가 있었다.

- **네이버페이 문자 다섯 건의 가맹점이 전부 `naver.me` 였다.** 본문 끝의
  `http://naver.me/PayO` 에서 도메인이 가장 긴 토큰이라 뽑혔다. **서로 다른 가게가
  한 이름으로 뭉치면 `catMemory` 까지 같이 오염된다** — 올리브영에서 고친 분류가
  티머니·한끼통살에 그대로 붙는다. 링크는 통째로 지운다. 스킴이 없는 주소 꼴
  (`ALIEXPRESS.COM`)은 남긴다 — 그건 진짜 가맹점 이름이다.

- **문자 제목이 `⁨1588-3819⁩` 로 온다.** 앞뒤에 U+2068/U+2069(방향 격리)가 붙어 있다.
  눈에는 안 보이지만 이름에 남으면 같은 가게가 매번 다른 이름이 되어 중복 판정도
  분류 기억도 어긋난다. `Parser.WIDE_SPACE` 에 방향 제어 문자 전부를 넣었다.

- **교통 낱말이 `guessCat` 과 `guessSubCat` 사이에서 어긋나 있었다.** 티머니·쏘카·
  통행료·렌터카가 앞쪽에만 있어서 여가로는 가는데 세부가 `문화/컨텐츠` 로 떨어졌다.
  **두 목록은 짝이다. 한쪽에 낱말을 넣으면 반대쪽도 봐야 한다.**

- **카카오페이 송금 세 건 73,100원이 아예 안 들어가고 있었다.** 이 앱은 조사로 방향을
  가리는데(`님에게`=내가 보냄, `님이`=내가 받음) **카카오페이는 상대를 제목에 두고
  본문에는 조사 없이 내 행동만 적는다** — 제목 `김다은`, 본문 `13,700원을 보냈어요`.
  `Parser.SENT_PLAIN`(`원을 보냈`)을 뒀다. **`보냈` 만 보면 안 된다** — 이 기록의
  `보냈` 62건은 대부분 인스타 릴스와 카톡 사진이다. 금액이 바로 앞에 붙은 꼴만 본다.
  `님이` 가 있으면 방향이 뒤집히므로 그때는 수입 규칙에 넘긴다.

### 규칙을 고쳤으면 로그를 통째로 다시 흘려 볼 것

판정을 바꾸면 **다른 판정이었던 건이 지출로 넘어온다.** 그게 다른 앱이 이미 알린
같은 결제면 두 줄이 된다. 오늘 취소를 좁혔더니 한끼통살 59,800원이 취소에서 지출로
바뀌었는데, 같은 결제가 카카오톡 두 번·네이버페이 문자 한 번 해서 세 번 온다.
`ReplayProbe` 가 시간순으로 흘려 `Store.isDuplicate` 까지 통과시킨다. 마지막 실측:

```
지출 알림에서 가계부에 들어간 줄: 44 · 중복으로 막힌 것: 21 · 놓친 중복: 0
```

남은 기타 15건은 `한끼통살`, `호맥 예천점`, `금솥` 같은 **진짜 상호** 다. 규칙에 상호를
넣기 시작하면 끝이 없어서 일부러 두었다(불변식 9 — 한 번 고치면 `catMemory` 가 기억한다).
`ParserTest.술집도_식비로_본다` 가 그 경계를 못 박고 있다.

### 분류 품질을 재는 법

고칠 곳을 감으로 찾지 말 것. 알림 기록 CSV 를 규칙에 통째로 흘려 세면 어디가 무너졌는지
바로 나온다. 프로브는 스크래치패드에만 두고 **저장소에 올리지 않는다**(raw.csv 가
사용자 개인 기록이다). 이 세션에서 쓴 프로브 셋:

| 프로브 | 재는 것 |
|---|---|
| `RealLogProbe` | 전체 판정 분포, 기타로 떨어진 가맹점 상위 목록, 안 잡힌 사유별 건수 |
| `RowProbe` | 낱말로 고른 알림 한 건씩의 판정과 뽑힌 가맹점 |
| `CancelProbe` | 취소로 잡힌 알림에서 취소어가 본문 어디에 있는지 |

세 번째 것이 규칙을 정하는 근거가 됐다 — **위치로 자르려다 실제 문구를 재 보고**
(토스는 `1,400원 결제 취소` 로 금액이 먼저 온다) 위치가 아니라 **붙어 있는 낱말** 로
가르기로 바꿨다.

### 아직 남은 것

- ~~삼성전자서비스 수리비의 가맹점이 `설문조사` 다~~ — 고쳤다. **본문이 120자를 넘으면
  제목을 먼저 본다**(`Parser.pickName`). 카드 알림은 본문이 짧고 그 안에 가맹점이
  있지만, 안내 문자와 주문 확인은 본문이 문단이라 엉뚱한 낱말이 뽑힌다. 제목이
  `결제 완료` 처럼 쓸모없으면 걸러져 빈 값이 되고 그때 본문으로 되돌아오므로 잃는 것이 없다.
- ~~`monthRemain` 은 여전히 고정지출을 안 뺀다~~ — 사용자가 빼라고 했고, 뺐다.
  홈 큰 숫자·상태창·위젯이 전부 이 함수를 지나므로 세 곳이 한꺼번에 같이 바뀐다.
  예산 띠도 같은 기준으로 고정지출을 저축과 함께 잠근다(홈·예산 탭 둘 다).
  `StatsTest.남은_돈은_고정지출을_미리_뺀다` 가 못 박고 있다.

  **그런데 사용자 눈에는 한동안 안 바뀐 것으로 보였다.** 코드가 아니라 갱신 문제였다 —
  아래 "위젯과 상태창은 스스로 안 고쳐진다" 를 볼 것.
- ~~`아크(ARK)PC방` 이 `PC방` 으로 줄어든다~~ — 고쳤다. 괄호를 **공백이 아니라 빈
  문자열로** 지운다. 공백으로 지우면 이름 한가운데 괄호가 토큰을 쪼개고 가장 긴 쪽만
  남는다. 붙이고 나니 카드 단말이 붙인 밑줄 꼬리가 이름에 눌어붙어서(`카카오T일반택시
  (법인)_3`) 밑줄도 구분자에 넣었다. 지금은 `아크PC방` · `카카오T일반택시` 로 나온다.
- **개별 상호(`금솥` `호맥 예천점` `연코리아`)는 여전히 기타다.** `호프`·`포차`·
  `이자카야` 같은 **업종 낱말**은 규칙에 넣었지만 가게 이름은 안 넣는다. 규칙을 상호로
  채우기 시작하면 끝이 없고, 남의 가게 이름을 잘못 삼키는 쪽이 더 위험하다.
  사용자가 한 번 고치면 `catMemory` 가 기억한다(불변식 9).
- **토스뱅크 이자 `394원을 통장에 쏙! 넣어드렸어요`** 가 미처리로 남는다. 수입이지만
  `이자` 를 규칙에 넣으면 `대출이자`(지출)와 부딪힌다. 값이 작아 미뤄 뒀다.
- **키움증권 체결통보** `매수106주 평균단가21,280원` — **안 넣기로 했다.**
  `매수(\d+)주 평균단가([\d,]+)원` 으로 정확히 뽑을 수 있어서 파싱 자체는 어렵지 않다.
  문제는 그다음이다. 106 × 21,280 = 2,255,680원이 투자로 기록되면
  `dailyBudget` 의 `월예산 + 수입 − 고정지출 − 투자` 에서 그만큼 빠져 **하루 한도가
  0이 된다.** 입금 150만원이 하루 한도를 밀어 올린 것과 정확히 같은 사고를 반대
  방향으로 내는 셈이다(불변식 15 참고).

  주식 매수는 쓴 돈이 아니라 옮긴 돈이다. 이 앱은 "알림만 읽는 가계부" 이고 자산
  추적기가 아니므로, 넣으려면 예산 계산에서 큰 투자를 어떻게 다룰지부터 정해야 한다.
  그 결정 없이 파싱만 붙이면 사용자가 주식 한 번 사고 앱을 못 쓰게 된다.

---

## 2. 빌드

```bash
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"
./gradlew assembleDebug
```

- Gradle 8.14.3 / AGP 8.13.0 / Kotlin 2.2.20 / compileSdk 36 / minSdk 26 / JDK 21(JBR)
- **APK 는 빌드가 끝나면 자동으로 `<루트>/가계부.apk` 에 복사된다.**
  `app/build.gradle.kts` 끝의 `assembleDebug.doLast` 가 한다. 사용자 요청이므로 유지할 것.
- 배포용 사본은 `pushledger.apk` 다. `.gitignore` 가 `*.apk` 를 막으므로
  `git add -f pushledger.apk` 로 넣어야 한다. 작업이 끝나면 **커밋 전에 새로 빌드해서 복사**할 것.

### 서명

`keystore.properties`(gitignore 대상)에서 비밀번호를 읽는다. 파일이 없으면 서명 설정을
아예 만들지 않으므로 `assembleRelease` 가 서명 없는 APK 를 낸다. 이 파일과
`release.keystore` 는 **절대 커밋하지 말 것.** 커밋 전 `git status --short | grep -i keystore`
로 확인하는 습관을 들일 것.

### 유닛 테스트는 ASCII 경로에서만 돈다

프로젝트 경로에 한글(`문서`)이 있어서 `testDebugUnitTest` 가 이 자리에서는
`ClassNotFoundException` 으로 실패한다. 클래스는 정상 컴파일되고 APK 빌드도 정상이다.
Gradle 테스트 워커만의 문제다. 이 세션에서 쓴 방법:

```bash
SP=<ascii-temp-dir>
# 최초 1회: gradle 래퍼와 설정 복사
cp -r <루트>/gradle "$SP/" && cp <루트>/gradlew <루트>/gradlew.bat \
      <루트>/settings.gradle.kts <루트>/build.gradle.kts <루트>/gradle.properties "$SP/"
echo "sdk.dir=C:/Android/Sdk" > "$SP/local.properties"
# app/build.gradle.kts 는 서명 설정과 APK 복사 태스크를 뺀 사본을 쓴다
# 매번: 소스만 다시 복사하고 돌린다
rm -rf "$SP/app/src" && cp -r <루트>/app/src "$SP/app/"
cd "$SP" && ./gradlew testDebugUnitTest
```

결과는 `$SP/app/build/test-results/testDebugUnitTest/*.xml` 에 있다.
**콘솔이 한글을 깨뜨리므로** 실패 메시지는 파이썬으로 XML 을 파싱해 읽는 편이 빠르다.

---

## 3. 깨면 안 되는 불변식

코드를 고치기 전에 이 목록을 먼저 볼 것. 전부 실제 버그를 겪고 세운 규칙이다.

1. **`Stats.active()` 가 모든 지출 집계의 유일한 관문이다.**
   취소분·투자분·고정지출 실행건(`by=="fixed"`)을 여기서 걸러낸다. 우회해서 직접
   `filter` 를 쓰면 취소 처리가 조용히 샌다.

2. **`Store.addTxn()` 이 거래 삽입의 유일한 경로다.**
   중복 판정(`isDuplicate`)이 여기 하나뿐이다. **금액과 시각만 본다** — 앱과 가맹점은
   일부러 안 본다. 한 번 결제하면 카카오페이·카드사·은행이 각자 알림을 띄우는데
   표기가 전부 달라서, 앱·가맹점을 조건에 넣으면 정작 막아야 할 것을 하나도 못 막는다.

   **예외는 `Store.importCsv()` 하나다.** 복원은 달별로 모아 한 번씩 써야 하고
   (줄마다 쓰면 24개월치가 파일 쓰기 수천 번 + 위젯 갱신 수천 번이 된다), `addTxn`
   의 10초 규칙을 그대로 쓰면 되살리려던 기록이 되살리는 도중에 뭉쳐 사라진다.
   거기서는 날짜·시각·금액·가맹점이 통째로 같을 때만 이미 있는 것으로 본다.
   **삽입 경로를 새로 만들 거면 이만한 이유가 있어야 한다.**

   **지우고 다시 넣는 자리에서는 `addTxn` 을 쓰지 말 것.** 내역 편집에서 거래를 다른
   달로 옮길 때 `deleteTxn` → `addTxn` 을 부르고 있었는데, 옮겨 간 달에 같은 금액이
   비슷한 시각에 있으면 `addTxn` 이 false 를 돌려주고 **그 거래는 이미 지워진 뒤라
   통째로 사라진다.** 편집은 시각을 원래대로 지키므로 매달 같은 시간에 같은 금액을
   쓰는 결제일수록 잘 걸린다. 옮기기는 새 결제가 아니라 있던 줄의 이사이므로
   `restoreTxn`(중복 판정 없음, 같은 id 면 안 넣음)을 쓴다.

3. **`Store.addFixed()` 가 고정지출 등록의 유일한 문이다.**
   등록과 동시에 그달의 같은 이름 결제를 실행건으로 바꾼다. 이걸 안 지나면 같은 돈이
   소비에서 한 번, 고정지출 계획에서 한 번 빠진다.

4. **고정지출 예산은 `cfg.fixed` 합계 하나뿐이다.**
   `Stats.BUDGETABLE_CATS` 가 `Cat.HOUSING` 을 뺀다. 항목별 예산에도 배정하면 두 번 깎인다.

5. **결제 취소는 수입이 아니라 원 거래의 무효화다.** `Txn.canceled` 를 세우고 레코드는 남긴다.

6. **`Store.writeMonth()` 가 상태창·위젯 갱신을 트리거한다.** 갱신 호출을 흩뿌리지 않는다.

7. **위젯·알림 서비스는 진입 시 `Store.ensure(context)` 를 부른다.**
   앱을 한 번도 안 연 상태에서 위젯이 갱신되면 `lateinit root` 미초기화로 죽는다.

8. **가맹점 이름은 저장 전에 `Merchant.clean()` 을 지난다.**
   `NotifListener` 와 `AiWorker` 두 곳이 입구다. 여기서 안 다듬으면 통계·반복 결제·
   취소 대조가 전부 결제사가 붙인 껍데기를 안고 간다.

9. **사용자가 정한 분류(`Config.catMemory`)가 규칙보다도 AI 보다도 우선한다.**
   `Store.recallCategory()` 를 먼저 보고 없을 때만 짐작한다. 이 순서를 뒤집으면
   사용자가 고쳐 놔도 다음 결제에서 도로 틀린다.

   **열쇠가 발밑에서 움직이는 것도 같은 약속을 깬다.** `catMemory` 의 열쇠는 그때그때의
   가맹점 추출 규칙에서 나온다. 규칙을 손보면 같은 가게의 열쇠가 달라져 기억이 조용히
   안 맞게 된다 — 이 세션에서 `PC방` 이 `아크PC방` 으로, `완료되었습니다` 가 `한끼통살`
   로 바뀌었다. 그래서 `recallCategory` 는 정확한 열쇠가 없으면 `Merchant.same` 으로
   한 번 더 찾는다. **가맹점 추출을 고칠 때는 이 되찾기가 그 변화를 덮는지 볼 것.**

10. **알림함에서 한 일은 전부 되돌릴 수 있어야 한다.**
    무시·앱 끄기·AI 기록·정산 차감 모두 되돌리는 길이 있다. 새 처리를 넣을 때
    되돌리기를 같이 넣지 않으면 사용자가 그 버튼을 안 누른다. 일괄 처리에는
    **일괄 되돌리기**가 붙어야 한다.

11. **오래 걸리는 일은 진행·결과·이동 자유 세 가지를 갖춘다.**
    진행은 `AiJob` 하나로 보고해 어느 탭에서나 보이고, 결과는 `Config.lastAiRun` 에
    적어 앱을 껐다 켜도 남고, 일은 WorkManager/앱 수명 코루틴에서 돌아 화면을 떠나도 산다.

12. **디자인 토큰 밖의 값을 화면 코드에 쓰지 않는다.**
    - 색 → `ui/Charts.kt` 팔레트와 `CatColor`
    - 글자 크기 → `ui/Type.kt` 의 `T.Display/Amount/Title/Body/Caption` 다섯 단.
      입력칸만 `TField.Big/Normal/Small` 로 따로 둔다 — 손으로 치는 자리는 커서가
      보여야 해서 같은 위계의 읽기 글자보다 한 치수 크다. 그 차이를 `T` 에 섞으면
      다섯 단이 여덟 단이 된다. **화면 코드에 날 `sp` 값이 하나라도 있으면 위반이다**
      (`grep -rn "[0-9]\+\.sp" app/src/main/java/com/pushledger/ui/ | grep -v Type.kt`).
    - 글꼴 → `ui/Type.kt` 의 `Pixel` (MainActivity 에서 `LocalTextStyle` 에 한 번만 건다)
    - 도트 크기·간격 → `ui/Dots.kt` 의 `DOT` / `DOT_GAP`
    - 모서리 → 배지 4dp · 안쪽 10dp · 카드 16dp
    - 금액 문자열 → `Money.kt` 의 `won()` / `wonShort()`. **알림함 사유 줄과 메모까지
      전부 포함이다** — 열 곳이 `${amount}원` 으로 날 숫자를 찍고 있어서 `59800원` 처럼
      보였다. 유일한 예외는 `Nvidia.kt` 의 프롬프트다. 사람이 아니라 모델이 파싱할
      자리라 `59800원` 이 정확하다.
      검사: `grep -rn "amount}원" app/src/main | grep -v won(`

13. **홈에 경고 카드를 쌓지 않는다.** 앱을 여는 이유는 `오늘 가용 예산` 하나다.
    그 위에 카드를 하나 더 얹을 때마다 정작 보러 온 숫자가 아래로 밀린다.
    이 세션에서 줄을 셋 더하다 경고가 네 줄로 쌓인 적이 있다. 지금은 위에 둘까지다 —
    미처리 알림과 예산 경고(넘긴 것과 넘길 것 같은 것을 **한 카드에** 담는다).
    참고성 정보(`평소보다 큰 결제`)는 하루 예산 **아래**로 보낸다.
    새 카드를 붙일 자리를 찾는다면 그 아래다.

14. **도트 그래픽은 이 앱의 정체다.** 차트 라이브러리로 갈아타지 않는다.
    띠를 두껍게 만들 때 **알갱이를 키우지 말고 줄 수를 늘린다** — 사용자가 여러 번 지적했다.

15. **입금·수입 알림을 거래로 자동 기록하지 않는다.** 사용자가 명시적으로 요구했다.

    이유는 취향이 아니라 산수다. `Stats.dailyBudget()` 이 하루 한도를
    `월예산 + 수입 - 고정지출 - 투자` 로 잡으므로 **입금 한 건이 그대로 예산에 더해진다.**
    실기기 기록의 `1,500,000원 입금 / 정은실 → 내 토스뱅크 통장` 하나가 그 달 남은
    나흘 기준 하루 한도를 약 37만원씩 밀어 올렸고, 사용자는 그 거래를 손으로 지웠다.
    용돈·정산금·계좌 이체가 전부 같은 "입금" 문구로 오기 때문에 문구로는 가를 수 없고,
    금액 임계값을 두면 그 숫자가 곧 새로운 오차가 된다.

    **막는 자리는 다섯이다.** 처음에 셋만 막고 "다 했다" 고 적었는데 두 개를 더 찾았다.
    수입은 한 군데서 못 막는다 — 거래에 `Cat.INCOME` 이 붙을 수 있는 길을 전부 세야 한다.

    1. `NotifListener` 의 `Out.Income` 분기 — 알림함 '무시됨' 으로 보낸다.
    2. 짝을 못 찾은 `Out.Settle` — 미처리로 남겨 사용자가 고르게 한다.
    3. `AiWorker` 의 `Cat.INCOME` 판정.
    4. **`AiReview`** — 이미 기록된 지출을 수입으로 바꾸자는 제안. 적용하면 그 거래가
       `Stats.active()` 에서 빠지면서 동시에 `incomeTotal` 로 들어가 **한 번 눌러서
       예산이 두 번 흔들린다.**
    5. **`NotifListener` 의 지출 분기에서 `Parser.guessCat`** — 가맹점 이름에 `환급`·
       `캐시백`·`배당` 이 있으면 수입을 돌려준다. 파서가 '나간 돈' 이라고 판정한 건에
       그게 그대로 붙었다. 이름이 뭐라 하든 지출은 지출이라 `Cat.ETC` 로 내린다.

    예시 데이터(`seedTestData`)의 수입 표본도 뺐다. 앱이 실제로 만들 수 없는 줄이고,
    35만원이 예산에 더해져 예시 화면의 하루 한도까지 부풀렸다.

    **되돌릴 길을 같이 둬야 한다(불변식 10).** 입금은 알림함 '무시됨' 칸에 남는데,
    그 줄에는 원래 `직접 입력` 이 없었다. 그래서 정말 넣고 싶은 사람은 무시 취소 →
    미처리 → 직접 입력 세 걸음을 밟아야 했고, 거기 있는 `AI로 읽기` 는 AI 가 다시
    수입이라고 답해 곧장 무시로 돌아오는 **죽은 버튼**이었다. 지금은 `무시됨` 줄에
    `직접 입력` 이 있다. 새 처리를 `IGNORED` 로 보낼 때는 그 칸에서 할 수 있는 일이
    무엇인지 같이 볼 것.
    **`Parser` 는 그대로 수입으로 읽는다.** 알림함에 사유를 적어 주려면 무엇인지는
    알아야 하고, 기록할지 말지는 호출자가 정하는 것이 맞다.
    **사용자가 그 가맹점을 수입으로 정해 둔 경우(`catMemory`)는 예외다** — 불변식 9.

---

## 4. 밟으면 시간 나가는 함정

### 4-1. 셸 heredoc 과 파이썬으로 Kotlin 파일을 쓸 때
이 환경의 Bash heredoc 은 `<<'EOF'` 로 인용해도 백슬래시를 한 겹 먹는다.
파이썬 치환도 `\n`, `\"`, `\uFEFF` 가 한 겹 벗겨져 **소스에 보이지 않는 제어문자가 박힌다.**
이 세션에서 실제로 CSV 내보내기 함수가 그렇게 깨져 컴파일이 안 됐다.

- 정규식과 이스케이프가 든 Kotlin 코드는 **Edit/Write 도구로 직접** 쓸 것.
- 파이썬을 쓸 거면 `r'''...'''` 원시 문자열을 쓰고, `\u` 같은 건 `chr(92)+'uFEFF'` 로 만들 것.
- 고치고 나면 `grep -n` 으로 그 줄을 눈으로 확인할 것.

### 4-2. `local.properties` 는 슬래시로
`sdk.dir=C:/Android/Sdk`. 역슬래시는 Properties 문법에서 이스케이프로 먹혀 경로가 깨진다.

### 4-3. Compose `padding` 인자 조합
`padding(horizontal=, top=, bottom=)` 같은 조합은 없다.
`padding(all)` · `padding(horizontal, vertical)` · `padding(start, top, end, bottom)` 뿐이다.

### 4-4. 애니메이션 값을 Composable 본문에서 읽지 말 것
`.value` 를 본문에서 읽으면 **매 프레임 그 함수가 통째로 다시 불린다.**
`State<Float>` 를 그대로 들고 있다가 `Canvas` 람다 안에서 읽으면 다시 그리기만 한다.
`Charts.kt` 의 `grow()` 와 `BudgetBar` 가 그 패턴이다. 목록 안 애니메이션에서는
이 차이가 곧 스크롤 부드러움이다.

### 4-5. 알림 스몰 아이콘은 반드시 단색 벡터
`setSmallIcon(R.mipmap.ic_launcher)` 처럼 적응형 아이콘을 넣으면 Android 8 이상에서
**알림이 통째로 무시되거나 `Bad notification` 으로 죽는다.** 지금은
`R.drawable.ic_stat_budget`(단색 벡터)를 쓴다.

### 4-6. 커스텀 RemoteViews 알림은 실패를 잡을 수 없다
SystemUI 가 그릴 때 실패하므로 앱에서 예외로 감지할 방법이 없다. 그래서 상태창은
표준 알림 두 줄로 갔다. 진단 수단은 설정 › 상태창 알림의 `지금 띄워보기` + `diagnose()` 다.

### 4-7. `res/font/` 에는 폰트 파일만 들어간다
`README.txt` 를 넣었더니 `mergeDebugResources` 가 죽었다. 출처와 라이선스는
저장소 루트 `LICENSE-Galmuri.md` 에 적어 뒀다. **OFL 이 요구하는 고지라 지우면 안 된다.**

### 4-8. `TextStyle()` 을 통째로 새로 만들면 글꼴이 딸려 오지 않는다
`LocalTextStyle` 에 건 `Pixel` 이 상속되지 않아 그 줄만 시스템 폰트로 튄다.
지금 그런 자리가 둘 있고(순위 배지, 입력칸) 거기엔 `fontFamily = Pixel` 을 직접 적었다.
새로 만들 때는 `LocalTextStyle.current.copy(...)` 를 쓰는 편이 안전하다.

### 4-9. 자바 정규식의 `\s` 는 전각 공백을 공백으로 안 본다
카드사 알림에 U+3000 이 흔히 섞여 온다. `Parser.flat()` 이 이걸 편다.
알림 문자열을 새로 다루는 코드를 쓸 때 이 함수를 지나게 할 것.

### 4-10. 예시 데이터는 덮어쓰지 않는다
`seedTestData` 가 예전에 그 달 거래를 통째로 덮어썼다(실사용 데이터 소실 버그).
지금은 뒤에 붙이고 `Store.SEED_TAG` 표식을 남겨 `clearTestData()` 가 그것만 지운다.

### 4-12. 금액 표기는 `원` 만이 아니고, 같은 결제는 여러 앱에서 온다
삼성 월렛(삼성페이)은 `₩7,100 결제 완료` 로 보낸다. **원 기호가 앞에 붙고 '원' 글자가 없다.**
새 결제 앱을 붙일 때는 그 앱이 금액을 어떤 꼴로 적는지 원문부터 확인할 것.

**더 중요한 것은 그다음이다.** 한 번 결제하면 삼성페이·토스·카드사가 각자 알린다.
못 읽던 꼴을 읽게 만들면 그만큼 **중복이 새로 생긴다.** 실기기 기록에서 삼성페이와
토스는 대개 2초 안에 붙어 왔지만 58초·59초·85초 벌어진 건이 셋 있었다.
파서를 넓힐 때는 `Store.isDuplicate` 가 그 간격을 덮는지 **같은 로그로 확인할 것.**
`한 건도 안 잡히던 앱을 잡히게 했다` 는 것만으로는 좋아진 것이 아니다.

### 4-11. RemoteViews 는 `@RemoteView` 가 붙은 클래스만 부풀린다
위젯 도트를 맨 `<View>` 로 그렸더니 홈 화면에 올릴 때 런처가 **"위젯을 추가할 수 없습니다"**
를 띄웠다. 런처는 위젯을 붙이면서 `initialLayout` 을 **자기 프로세스에서** 부풀리는데,
`android.view.View` 는 허용 목록에 없어 거기서 통째로 실패한다. 앱은 죽지도 않고
`runCatching` 에도 안 걸린다(4-6 과 같은 이유). 지금은 `ImageView` 로 그린다.
**위젯 레이아웃에 새 태그를 넣을 때는 그 클래스에 `@RemoteView` 가 있는지 먼저 볼 것.**
쓸 수 있는 것: `LinearLayout` · `RelativeLayout` · `FrameLayout` · `GridLayout` ·
`TextView` · `ImageView` · `Button` · `ImageButton` · `ProgressBar` · `Chronometer` 등.
`ConstraintLayout` 과 맨 `View` 는 안 된다.

같은 자리에서 `daily_widget_info.xml` 의 `targetCellHeight` 도 1에서 2로 올렸다.
`minHeight` 가 90dp 인데 1칸(약 40dp)을 달라고 하고 있었다. Android 12 이상 런처는
`targetCell*` 을 먼저 보므로 그대로 두면 도트 줄과 아랫줄이 잘린다.

---

## 5. 알아 두면 좋은 구조

### 알림 → 거래
`NotifListener.handle()` 하나가 전부다. 순서가 곧 정책이라 순서를 바꾸면 뜻이 바뀐다.
차단 앱 → dedup → 로그 함수 정의 → 허용 목록 밖 → `Parser.parse()` 분기.
**켠 앱이든 아니든 차단하지 않은 앱의 알림은 전부 알림함에 남는다.**

### `Parser.parse()` 의 분기 순서 (바꾸면 뜻이 바뀐다)
취소 → 충전 → 배당 → 정산 → 상대 수령 → 내가 보낸 송금 → 내가 받음 → 지출 아님 → 결제.
예를 들어 "상대 수령" 이 "내가 받음" 보다 앞에 있어야 하는 이유는 둘 다 "받았" 을
포함하기 때문이다. README 의 송금 방향 표를 먼저 읽을 것.

### AI 는 셋
| 하네스 | 무엇 | 파일 |
|---|---|---|
| `AiQueue`/`AiWorker` | 못 읽은 알림을 거래로 | `AiWorker.kt` |
| `ReviewRun`/`ReviewWorker` | 이미 기록된 것을 다시 검토 | `AiReview.kt` |
| `CoachRun` | 리포트 생성 | `Coach.kt` |

셋 다 진행 상황을 `AiJob` 에 보고한다. 새 AI 기능을 붙일 때 진행 표시를 새로 만들지 말 것.

`Nvidia.call()` 은 일 성격에 따라 설정이 갈린다 — **파싱은 생각 모드 끔**(400토큰, temp 0),
리포트는 켬(16384토큰, temp 0.3). 파싱에 생각 모드를 켜면 한 건에 수십 초가 걸린다.

### 내보내기와 되살리기는 한 쌍이다
`Store.csvRow()` 가 적고 `Store.parseCsv()` 가 되읽는다. **둘은 같은 열을 마주 본다 —
한쪽만 고치면 `CsvTest` 왕복 검사에서 깨진다.** 열 이름을 바꾸려면 두 곳과 테스트를 같이.

- **필드 안의 줄바꿈은 반드시 공백으로 편다.** 되읽기는 줄 단위로 자르므로, 메모에
  줄바꿈이 하나 들어가면 그 거래가 두 줄로 쪼개져 양쪽 다 파싱에 실패하고 조용히
  사라진다. 되살리려던 바로 그 데이터가 백업을 지나며 없어진다.
- 마지막 `예정` 열은 아직 안 나간 고정지출 자리표라는 표시다. 이게 없으면 복원한
  자리표를 `hasRealFixed` 가 '이미 나갔다' 고 읽어, 뒤에 오는 진짜 출금이 고정지출로
  안 묶이고 소비로 또 세어진다. 예전 파일에는 이 열이 없지만 그대로 읽힌다.
- 되읽기는 **순서가 아니라 열 이름으로** 자리를 찾는다. 사용자가 표 계산기에서 열을
  옮겨도 금액 자리에서 가맹점을 읽지 않는다. 필요한 열이 없으면 아예 안 읽는다.
- 깨진 줄은 버리고 나머지를 살린다. 한 줄 때문에 전부 포기하면 백업으로서 쓸모가 없다.
- `Store.importCsv()` 는 **덮어쓰지 않는다.** 없는 건만 채운다(불변식 2 예외 참고).
  예시 데이터가 예전에 그 달을 통째로 덮어써 실사용 데이터를 날린 적이 있다(4-10).

### 이 앱에서 숫자가 틀리는 방식은 하나다 — `total()` 을 `monthlyBudget` 과 견주는 것

`Stats.total()` 은 고정지출을 빼고 센다(불변식 1). 그런데 `cfg.monthlyBudget` 에는
그 몫이 들어 있다. **둘을 나란히 놓는 순간 늘 고정지출만큼 여유 있어 보인다.**
이 세션에서 같은 실수를 **다섯 곳**에서 찾았다.

| 자리 | 증상 |
|---|---|
| 위젯 `이대로면 월` | 넘길 판인데 회색으로 떴다 |
| 리포트 월말 예상 · 소진 예상일 | 리포트는 "남길 속도", 위젯은 "넘김" |
| 홈·예산 탭 예산 띠 | 70만 남은 것처럼 보이는데 실제로는 20만 |
| 리포트 프롬프트 | 모델이 `총 지출 ÷ 월 예산` 으로 재고 "여유롭다" 고 답한다 |
| `Stats.monthRemain` | 홈의 **가장 큰 숫자**. 띠는 고쳤는데 그 위 숫자를 안 고쳤었다 |

마지막 것이 제일 오래 살아남았다. 띠와 하루 한도를 차례로 고치면서도 바로 위의
큰 숫자는 못 봤다 — **같은 화면 안에서 고친 그림과 안 고친 숫자가 나란히 있었다.**
한 곳을 고칠 때 그 값을 쓰는 다른 자리를 `grep` 으로 같이 셀 것(아래 "쓰기 자리를
세는 법"). `monthRemain` 은 홈·상태창·위젯 세 곳이 쓴다.

**`total(month)` 을 무언가와 견줄 일이 생기면 `Stats.variableBudget(cfg)` 이 짝이다**
(`월 예산 − 고정지출 − 저축 목표`). 새 화면이나 새 계산을 붙일 때 `total` 과
`monthlyBudget` 이 한 식 안에 같이 있으면 의심할 것.

살아 있는 예산 엔진(`dailyBudget` · `spare` · `dailyVariableBudget`)은 여기 안 묶었다.
저축을 **목표**로 뺄지 **실제 투자액**으로 뺄지가 함수마다 다른데, 그건 "이번 달 계획"
과 "지금 남은 돈" 이라는 다른 질문이라 일부러 다르다. 묶으려 하지 말 것.

### 월말 예상은 한 함수에서만 나온다
`Stats.monthPace(cfg, month, today)` 하나를 홈·위젯·리포트(`Coach`)가 같이 쓴다.
**변동 소비만 경과일로 늘리고 고정지출은 계획 금액을 그대로 더한다.** 월세는 달에 한 번
나가는 돈이라 하루 평균으로 펴면 말일에 몇 배가 되고, 반대로 빼고 재면 고정지출 몫이
든 월 예산과 견줄 수 없어 늘 여유 있어 보인다.

세 곳이 각자 세던 때가 있었다. 위젯과 `Coach` 가 따로 `perDay × 총일수` 로 셌고 둘 다
고정지출을 안 더한 채 월 예산과 견줬다. 그래서 **리포트는 "남길 속도" 라고 하고
위젯은 "넘김" 이라고 하는 일**이 생겼다. 새로 예상치를 쓸 자리가 생기면 이 함수를 쓸 것.

`Coach` 의 **예산 소진 예상일**만은 변동 예산(`월예산 − 고정지출`)으로 잰다.
고정지출은 하루하루 새어 나가는 돈이 아니라 통째로 빠지는 돈이라, 월 예산 전체를
하루 소비 속도로 나누면 실제보다 한참 늦은 날이 나온다.

### 고정지출은 언제 빠지나 — 미리 빼고, 나갈 때 또 빼지 않는다

하루 한도는 **달 첫날부터 고정지출 전액이 이미 빠진 상태로** 계산된다.
`Stats.dailyBudget()` 이 `fixedTotal(cfg)`(등록된 계획 합계)를 월 예산에서 먼저 뺀다.
실제로 돈이 나가는 날 한도가 또 줄어드는 일은 없다. 그걸 막는 것이 세 겹이다.

1. `Stats.active()` 가 `by == "fixed"` 를 거른다 → 실행건은 소비 집계에 안 들어간다.
2. `NotifListener` 가 등록된 고정지출과 이름이 맞는 알림에 `by = "fixed"` 를 박는다.
3. `Store.addFixed()` 가 등록 시점에 그달의 같은 이름 결제를 소급해 `by = "fixed"` 로 바꾼다.

**뚫리는 자리는 이름 매칭 하나뿐이다.** 카드사가 상호를 다르게 적어
`NotifListener` 의 `matchedFixed` 가 못 잡으면 그 건은 `by = "rule"` 로 들어가
소비로도 세어진다 — 계획으로 이미 뺐는데 또 빠지는 유일한 경로다.

주의: **"이달 남은 돈" 두 가지가 뜻이 다르다.**
- `Stats.monthRemain()` = 월 예산 − 고정지출 − 변동 소비. 홈 큰 숫자·상태창·위젯이 이걸 쓴다.
- `Stats.spare()` = 월 예산 + 수입 − 고정지출 − 소비 − 투자. 저축 목표 카드 전용이다.

둘 다 고정지출을 뺀다. 수입과 투자를 어떻게 보느냐만 다르다.

그래서 **월 예산과 견주는 값에는 반드시 고정지출을 같은 쪽에 넣어야 한다.**
`Stats.monthPace()`(월말 예상)가 변동 소비만 늘리고 고정지출은 계획 금액을 그대로
더하는 이유다. 처음에는 고정지출을 뺀 채 월 예산과 견주어, 넘길 판인데도 회색으로 떴다.

### 위젯과 상태창은 스스로 안 고쳐진다

**계산을 고쳐도 위젯과 상태창은 예전 숫자를 그대로 들고 있는다.** 실제로 사용자가
"위젯이랑 상태바에는 적용 안 했잖아" 라고 한 일이 있고, 그때 코드는 이미 맞았다.

앱 안 화면은 Compose 가 상태를 구독하니 값이 바뀌면 알아서 다시 그린다. 위젯과
상주 알림은 아니다. 둘 다 **누가 다시 그려 줘야만** 바뀌는 그림이다.

| 다시 그려지는 때 | 누가 |
|---|---|
| 앱을 열 때 | `MainActivity` 의 `LifecycleResumeEffect` → `StatusNotifier.update` |
| 거래·설정이 바뀔 때 | `Store` 안의 세 자리에서 `StatusNotifier.update` |
| 30분마다 | `daily_widget_info.xml` 의 `updatePeriodMillis` (위젯만) |
| 위젯 크기를 바꿀 때 | `onAppWidgetOptionsChanged` (그 위젯 하나만) |
| **새 APK 를 깔았을 때** | `MY_PACKAGE_REPLACED` → `DailyWidgetProvider.onReceive` |
| **폰을 재시작했을 때** | `BOOT_COMPLETED` → 같은 자리 |

마지막 두 줄이 오래 비어 있었다. 앱을 새로 깔면 시스템이 상주 알림을 지우고 위젯은
마지막 그림에서 굳는다. 이 앱은 **새 빌드를 자주 까는 앱**이라, 계산을 고쳐 놓고
APK 를 건네면 사용자는 앱을 다시 열기 전까지 옛 숫자를 본다 — 안 고쳤다고 읽힌다.

`StatusNotifier.update()` 는 끝에서 `DailyWidgetProvider.updateAll()` 을 부르므로
**상태창을 갱신하면 위젯도 같이 갱신된다.** 둘을 따로 부를 일은 없다.

이 자리는 브로드캐스트 안이라 `Store.ensure()` 로 저장소부터 연다. 앱 프로세스가
죽은 뒤 깨어난 것일 수 있고, 안 열고 읽으면 기본값(예산 0)이 그려진다.

### 위젯 크기 대응
`WidgetPlanner.plan(폭dp, 높이dp, cfg)` 하나가 **무엇을 펼칠지 전부 정한다.**
크기마다 레이아웃 파일을 따로 두지 않고 줄은 한 벌만 두고 `setViewVisibility` 로 접는다 —
네 벌을 고치다 한 벌을 빠뜨리면 그 크기에서만 조용히 틀리기 때문이다.

- 접는 순서는 **위에서 재는 높이(dp)** 가 정한다. 칸이 줄어들수록 이 순서로 사라진다.

  | 높이 | 이때 사라지는 것 |
  |---|---|
  | 180 아래 | 도트 둘째 줄 |
  | 140 아래 | 월말 예상 |
  | 100 아래 | 도트 띠 통째로 |
  | 90 아래 | 오늘 지출·한도 줄 |
  | 70 아래 | 이달 남은 돈 |

  큰 숫자와 배지는 1x1 에서도 남는다. 폭은 따로 본다 — 110 아래면 배지·월·오늘 줄이
  같이 빠지고, 180 아래면 월말 예상이 빠진다.
- 사용자가 설정에서 끈 것은 **칸이 아무리 넓어도 되살아나지 않는다.** 크기는 켠 것을
  접기만 하고 끈 것을 펴지 않는다.
- 런처가 주는 값 중 **높이는 `MAX_HEIGHT`, 폭은 `MIN_WIDTH`** 를 본다. 이름과 달리
  `MIN_HEIGHT` 는 가로 화면일 때의 높이라, 세로로 쓰는 폰에서 그대로 믿으면 실제보다
  한참 낮게 잡혀 도트가 통째로 사라진다.
- 경계값은 `WidgetPlannerTest` 가 지킨다. 숫자를 만질 때는 테스트를 같이 고칠 것.
- 도트는 두 줄까지다. 세 줄이 필요하면 레이아웃에 `wd32` 부터 열여섯 개를 더 깔고
  `WIDGET_DOTS` 를 늘려야 한다.

### 저장소
`ledger/YYYY-MM.json`(월별) · `inbox/YYYY-MM-DD.json`(일별) · `config.json` · `fixes.json`.
알림함은 메모리에 최근 3,000건만 들고 있고 파일에는 보관 기간만큼 다 남는다.
**내보내기는 파일에서 직접 읽으므로** 메모리 상한에 안 잘린다.

보관 기간은 **두 칸**이다. 잡담(`keepInboxDays`, 기본 30일)과 돈이 걸린 알림
원문(`keepMoneyDays`, 기본 90일). `Store.sweep()` 이 둘 다 지난 날은 파일째 지우고,
잡담만 지난 날은 파일을 다시 써서 `Raw.isMoney` 인 줄만 남긴다.

`Raw.isMoney` 는 **남길 것이 아니라 버릴 것을 센다**(`IGNORED` 와 `NOISE` 만 잡담).
상태가 새로 생겼을 때 목록에 넣는 것을 잊으면, 남길 것을 세는 쪽은 새 상태의 원문을
조용히 일찍 지운다. 반대로 적으면 잊어도 오래 남을 뿐이다 — 틀릴 거면 이쪽으로 틀린다.

**화면이 드는 창과 파일에 남기는 기간은 다르다.** `init` 은 `keepInboxDays` 만큼만
읽는다. 여기에 `keepMoneyDays`(최대 1년)를 쓰면 앱을 켤 때마다 날짜 파일을 그 수만큼
열어 파싱하고, 위젯 갱신처럼 브로드캐스트에서 `ensure()` 가 도는 자리에서는 그게 곧
ANR 이다. **오래된 결제 원문은 화면이 아니라 `알림 기록 CSV` 로 꺼낸다** — 내보내기는
파일에서 직접 읽으므로 이 창에 안 잘린다. 실제로 이 세션에서 한 번 그렇게 넣었다가
되돌렸다. `sweep()` 은 파일을 지운 뒤 `inbox` 목록에도 같은 규칙을 적용한다. 안 그러면
설정에서 기간을 줄여도 화면에는 이미 없는 줄이 남아 눌러도 아무 일이 안 일어난다.

---

### 쓰기 자리를 세는 법 — 파일을 읽는 것보다 정확하다

이 세션 후반의 버그는 전부 **파일을 훑어서가 아니라 API 를 세어서** 나왔다.
1,700줄을 읽는 대신 `grep` 한 줄로 자리를 전부 세고 그중 위험한 것만 본다.

```bash
# 거래를 쓰는 자리 (22곳) — 여기서 다른 달 이사 중 거래가 사라지던 것을 찾았다
grep -rn "Store\.\(addTxn\|updateTxn\|deleteTxn\|restoreTxn\|writeMonth\|importCsv\|applyFix\|undoFix\|applySettlement\|applyCancel\|replaceAutoFixed\|addFixed\)" app/src/main
# 설정을 쓰는 자리 (28곳)
grep -rn "saveConfig" app/src/main
# 파일을 진짜 지우는 자리 (3곳) — 되돌릴 수 없는 유일한 연산
grep -rn "\.delete()\|deleteRecursively" app/src/main
# 거래에 수입이 붙을 수 있는 자리 (불변식 15)
grep -rn "Cat.INCOME" app/src/main
# 알림함에서 상태를 바꾸는 자리 (17곳) — 하나하나 되돌리기가 짝을 이뤄야 한다(불변식 10)
grep -n "Store\.\(setRawState\|ignoreAllFrom\|restoreAll\|restoreAllFrom\|deleteTxnByDedup\)" app/src/main/java/com/pushledger/ui/Inbox.kt
```

불변식 10은 2026-09-06 에 이 방법으로 훑어 짝이 다 맞는 것을 확인했다. 일괄 처리
두 개(AI 일괄 기록·일괄 무시)에도 일괄 되돌리기가 붙어 있다.

이 넷을 돌려 보면 `Charts.kt`·`StatsScreen.kt`·`AiScreen.kt`·`MainActivity.kt` 는
거래를 아예 안 건드린다는 것도 같이 보인다. **"안 열어 본 파일이 있다" 는 걱정은
파일 목록이 아니라 이 목록으로 푼다.**

## 6. 남은 작업

### 근거가 있고 값이 확실한 것
- ~~허용 앱마다 몇 건이 잡히는지 세어 볼 것~~ — **넣었다.** 앱 관리 화면의 각 줄에
  `알림 84건 · 기록 0건` 이 뜬다. **금액이 보이는 알림이 왔는데 한 건도 안 잡혔을 때만**
  빨갛게 경고한다 — 그냥 0건인 것은 이상하지 않다(네이버 알림 열 건은 전부 인증·예약
  안내라 잡힐 것이 없었다).

  금액이 보이는지는 저장된 상태가 아니라 **지금 규칙으로 다시 센다.** 저장된 상태로
  세면 정작 찾고 싶은 경우를 놓친다 — 삼성 월렛 84건은 그때 `₩` 를 못 읽어서 전부
  '금액 없음' 으로 저장돼 있었다. 규칙이 좋아진 뒤 다시 세야 드러난다.

  이 화면이 있었으면 삼성페이가 몇 달 동안 0건이던 것을 첫날 봤다.

### 사용자가 미룬 것
- **애니메이션 확대** — 지금은 차트가 자라는 것, 도트 띠의 음영 물결, 카테고리 칩의
  도트 부스러기뿐이다.

### 해 볼 만하지만 급하지 않은 것
- **예산 소진 예정일을 홈에도** — 리포트(`Coach`)에는 이미 있다. 변동 예산을 하루
  소비 속도로 나눠 잰다. 홈에 한 줄 붙일지는 화면이 이미 빽빽해서 미뤄 뒀다.

### 하지 말 것 (검토하고 버린 것)
- **형태소 분석기(Nori/KOMORAN)** — 사전만 수십 MB. 조사 처리는 `Hangul.kt` 의
  받침 규칙으로 충분하다.
- **푸리에/생성모형 주기성 탐지** — 표본 3~6개짜리 월 단위 이벤트에 과하다.
  지금의 간격 25~35일 규칙이 같은 일을 한다.
- **ML 분류기** — 학습 데이터가 없고, `catMemory`(사용자 교정 기억)가 그 자리를 채웠다.
- **오픈뱅킹 API** — "알림만 읽는다" 는 이 앱의 전제를 부순다.
- **증권 체결통보를 거래로 넣기** — 파싱은 쉽지만 220만원짜리 매수 한 건이 하루 한도를
  0으로 만든다. 주식 매수는 쓴 돈이 아니라 옮긴 돈이다. 자세한 이유는 §1 에 적었다.
- **가맹점 규칙을 개별 상호로 채우기** — `호프`·`포차` 같은 **업종 낱말**은 넣되
  `금솥`·`호맥` 같은 가게 이름은 넣지 않는다. 끝이 없고, 남의 가게를 잘못 삼키는 쪽이
  더 위험하다. 한 번 고치면 `catMemory` 가 기억한다(불변식 9).
- **입금을 거래로 기록하기** — 불변식 15. 막는 자리가 다섯이라 하나만 풀어도 샌다.

---

## 7. 사용자와 일하는 법

- **한국어로 답한다.**
- 요구가 한 메시지에 여러 개 섞여 들어오고, 작업 중에도 추가된다.
  전부 처리하고 무엇을 했는지 항목별로 답하는 방식이 잘 맞았다.
- **직접 확인하지 않은 것을 됐다고 말하지 않는다.** 사용자가 실기기로 검증하며
  "아직 안 됨" 을 여러 번 지적했다. 확인한 것과 못 한 것을 나눠 말하면 신뢰가 유지된다.
- **지적이 거칠게 온다.** 욕이 섞여도 그 안에 정확한 기술적 지적이 있다.
  변명하거나 사과를 늘어놓지 말고, 원인을 코드에서 짚고 고쳐서 보여 줄 것.
  이 세션에서 "개똥임", "제대로 좀 해" 같은 말이 나온 자리는 전부 실제로 코드가 틀린 자리였다.
- **추측으로 된다고 하지 말 것.** 기기가 없으면 없는 대로, 알고리즘만이라도 JVM 에서
  돌려 눈으로 보여 주는 편이 훨씬 잘 통했다(도트 폰트 ASCII 렌더링이 그 예다).
- 사용자는 도트 그래픽에 강한 취향이 있다. "바 말고 도트", "작은 게 예쁨", "빽빽하게"
  같은 지적이 반복됐다. 알갱이를 키우는 방향은 거의 항상 거절당했다.
- 코드 스타일은 ponytail — 최소 의존성, 최소 추상화. 차트 라이브러리·DI·Room 전부
  일부러 안 썼다. **주석은 "무엇" 이 아니라 "왜" 를 적는다.** 이 저장소의 주석 밀도가
  기준이다. 특히 어떤 규칙을 왜 그렇게 정했는지, 예전에 어떻게 틀렸는지를 남긴다.
- 작업이 끝나면 **빌드해서 APK 를 파일로 건네고, GitHub 에 푸시하고, 다운로드 주소를 준다.**
- 인터넷에서 근거를 찾아오는 것을 좋아한다. 다만 "개소리 같은 아이디어" 는 싫어한다 —
  **지금 실제로 깨져 있는 것**과 **근거가 있는 것**만 가져올 것. 버린 것과 버린 이유를
  같이 말하면 신뢰가 올라간다.

---

## 8. 다음 에이전트가 부를 만한 스킬

| 스킬 | 언제 |
|---|---|
| `ponytail:ponytail` | 코드를 쓰거나 고치기 전에. 이 코드베이스 전체가 그 스타일이라 켜지 않으면 결이 어긋난다. |
| `diagnosing-bugs` | 실기기에서 알림이 안 잡히거나 상태창·위젯이 안 뜰 때. 증상만 보고 고치면 이 프로젝트에서 여러 번 헛다리를 짚었다. |
| `code-review` | `Store`·`Stats` 를 고친 뒤. 알림 서비스와 UI 가 같은 저장소를 동시에 쓰므로 동시성 눈으로 볼 것. |
| `run` | APK 를 설치해 실제로 확인할 때. (지금 이 환경에는 기기도 에뮬레이터도 없다) |

`claude-api` 는 부를 필요 없다 — 이 앱은 Anthropic API 를 쓰지 않는다.
